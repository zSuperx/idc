[package]
name = "shrimple"
version = "0.1.0"
edition = "2024"

[dependencies]
registry = { path = "../registry", version = "0.1.0" }
bitset = { path = "../bitset", version = "0.1.0" }
smallvec = "1.16.0"
